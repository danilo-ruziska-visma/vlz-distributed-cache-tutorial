{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "CacheSettings": {
    "ConnectionString": "<your azure redis connection string here>",
    // "If you have a local redis installed, use it like below"
    // "ConnectionString": "localhost:6379" -- change the port with the one in your machine
    "DefaultSlidingExpirationInMinutes": 5
  },
  "AllowedHosts": "*",
  "AzureCosmo": {
    "EndpointUri": "<endpoint to azure cosmos db>",
    "PrimaryKey": "<primary key for azure cosmos db needed to connect>"
  }
}